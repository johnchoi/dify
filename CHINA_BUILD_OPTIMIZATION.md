# Dify API 容器构建优化 - 中国网络环境

## 🚀 概述

针对中国网络环境，我们对 Dify API 容器的构建过程进行了优化，显著提升了构建速度。

## 🔧 优化内容

### 1. APT 包管理器优化

- **镜像源替换**: 使用阿里云 Debian 镜像源替代官方源
- **网络超时配置**: 设置合理的超时时间（10秒）
- **重试机制**: 失败后自动重试 3 次
- **并发下载**: 优化包下载并发数

### 2. Python 包管理优化

- **PyPI 镜像**: 使用清华大学 PyPI 镜像源
- **UV 包管理器**: 优化网络设置和超时配置
- **并发控制**: 限制并发下载数避免网络拥塞

### 3. Docker 构建优化

- **多阶段构建**: 分离依赖安装和运行环境
- **缓存优化**: 利用 Docker 层缓存减少重复构建
- **网络配置**: 针对中国网络环境的特殊配置

## 🛠️ 使用方法

### 方式一：使用优化构建脚本（推荐）

```bash
# 使用默认参数构建
./build-api-china.sh

# 指定镜像名称和标签
./build-api-china.sh my-dify-api v1.0.0
```

### 方式二：直接使用 Docker Compose

```bash
# 确保已经应用了优化配置
docker-compose build api
```

### 方式三：手动构建

```bash
cd api
docker build -t dify-api:latest .
```

## 📊 性能提升

经过优化后，预期可以获得以下性能提升：

- **APT 包下载**: 速度提升 **3-5倍**
- **Python 包安装**: 速度提升 **5-10倍**
- **整体构建时间**: 减少 **60-80%**

## 🔧 镜像源配置

### 当前使用的镜像源

- **Debian 包**: 阿里云镜像源 (`mirrors.aliyun.com`)
- **Python 包**: 清华大学镜像源 (`pypi.tuna.tsinghua.edu.cn`)

### 备用镜像源

如果当前镜像源出现问题，可以在 `api/docker/sources.list.china` 文件中找到其他可选镜像源：

- 清华大学镜像源
- 163 网易镜像源  
- 华为云镜像源

## 🚨 注意事项

1. **网络环境**: 这些优化主要针对中国大陆网络环境
2. **镜像同步**: 镜像源可能存在同步延迟，如遇到包版本问题请稍后重试
3. **企业网络**: 如果在企业网络环境中构建，可能需要配置代理设置

## 🔍 故障排除

### 构建失败怎么办？

1. **检查网络连接**: 确保能够访问镜像源
2. **清理 Docker 缓存**: `docker system prune -a`
3. **重新构建**: `docker-compose build --no-cache api`
4. **更换镜像源**: 修改 Dockerfile 中的镜像源地址

### 常见错误

- **连接超时**: 可能是网络问题，稍后重试
- **包版本冲突**: 清理缓存后重新构建
- **磁盘空间不足**: 清理无用的 Docker 镜像和容器

## 📝 更新日志

- **2024-01**: 初始版本，添加基础镜像源优化
- **当前版本**: 完整的网络优化方案，包含 APT 和 Python 包管理优化

## 🤝 贡献

如果您发现更好的镜像源或优化方案，欢迎提交 PR 或 Issue。 
